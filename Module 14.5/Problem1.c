#include <stdio.h>
#include <string.h>
int main()
{
    int t;
    char str[100];
    scanf("%d", &t);

    while (t--)
    {
       //scanf("%s",str);
       gets(str);
       printf("%s",str);

    }
    //printf(" ");
     
}